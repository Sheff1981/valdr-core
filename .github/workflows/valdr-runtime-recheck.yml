name: Archived Bitcoin Core Runtime Recheck

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived-line-notice:
    runs-on: ubuntu-24.04
    steps:
      - name: Archived experiment notice
        run: |
          echo "The Bitcoin Core 31.1/C++ experiment is preserved on branch:"
          echo "archive/bitcoin-core-experiment-0.5.0"
          echo "Run legacy runtime gates from that archived branch only."
